var n,i,j,k,len,l:longint;
    s,tmp,s1:string;
    dir:array[0..100000] of string;
begin
	readln(n);
	for i:=1 to n do begin
		readln(s);
		delete(s,1,1);
		s:=s+'/';
		for j:=1 to
	end;
end.
