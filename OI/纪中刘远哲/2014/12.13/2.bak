const	maxn=100000;
var	i,j,k,m,n:longint;
	x,y,z:array[0..maxn] of longint;
	ch:char;
begin
	readln(n,x[0],y[0]);
	for i:=1 to n do begin
		read(x[i],y[i],ch,ch);
                //read(ch);
		if ch='\' then z[i]:=1 else z[i]:=2;
	end;
end.
