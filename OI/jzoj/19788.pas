var	i,j,k,n,m,u,v:longint;
	a:array[1..262144] of longint;
	c:array[1..100000] of longint;
	ch:char;
begin
	readln(n);
	for i:=1 to n do read(c[i]);
       //init;
	readln(m);
	for i:=1 to m do begin
		read(ch);
                read(u,v);
	end;
end.
